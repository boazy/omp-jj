--- a/zsh/custom/git.zsh
+++ b/zsh/custom/git.zsh
@@ -110,23 +110,23 @@
   return $?
 }
 
-# Wrapper for wt (simple worktree manager)
+# Wrapper for wt (unified workspace / worktree manager)
 function wt() {
-  local cmd
-  local dir
-  cmd="$1"
-  if [[ $cmd == "cd" ]]; then
+  local cmd="$1"
+  if [[ "$cmd" == "cd" ]]; then
     shift
-    dir="$(command wt select "$@")"
-    cd "$dir"
+    local dir
+    # Exit 2 (cancellation) or 1 (error) prevents cd
+    dir="$(command wt select "$@")" || return $?
+    [[ -n "$dir" ]] && cd "$dir"
+    return 0
   fi
-  # Call the wt binary, bypassing this function
   command wt "$@"
 }
 
-# cd to the main worktree, matching the current subdirectory when possible
+# cd to the primary checkout/workspace, mirroring the current subdirectory
 function wtm() {
   local dir
-  dir="$(command wtm "$@")" || return
-  cd "$dir"
+  dir="$(command wt main "$@")" || return $?
+  [[ -n "$dir" ]] && cd "$dir"
 }
